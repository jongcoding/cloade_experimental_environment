# Comment on atlas-2024-q1/web/sql-basic

Author: dev7
Posted: 2024-10-20

UNION-based SQLi는 컬럼 수 매칭이 핵심. ORDER BY 증가시키다가 에러 나는 지점
-1이 컬럼 수. 이 문제는 4개. 그 다음 NULL 패딩으로 각 컬럼 타입 맞춰주면 됨.

참고: 페이로드 앞 `'`가 닫히는 걸 주의. 뒤 `-- -`으로 주석 처리.
